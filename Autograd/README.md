## Topics Covered

- `requires_grad`
- `backward()`
- `.grad`
- Gradient Accumulation
- `zero_()`
- Computation Graph
- Chain Rule
- `torch.no_grad()`
- `detach()`

## Learning Outcome

After completing this practice, I understood how PyTorch automatically computes gradients, applies the chain rule, and stores gradients for model parameters during training. These concepts are essential for understanding backpropagation and training neural networks.
